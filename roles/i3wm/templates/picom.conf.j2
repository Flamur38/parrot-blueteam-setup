# === Shadows ===
shadow = false;

# === Fading — disabled to reduce lag ===
fading = false;

# === Opacity ===
inactive-opacity          = 1.0;
active-opacity            = 1.0;
frame-opacity             = 1.0;
inactive-opacity-override = false;

# === Corners ===
corner-radius = 0;

# === Backend ===
backend = "xrender";
vsync   = false;
